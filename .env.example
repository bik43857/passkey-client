# Base URL for the Spring Boot API. In local dev, Vite's proxy (vite.config.js)
# forwards /api to http://localhost:8080, so this can stay empty/relative.
# In production, point this at your deployed API origin if it's not served
# from the same origin as the frontend (see Section 18 / nginx.conf).
VITE_API_BASE_URL=
